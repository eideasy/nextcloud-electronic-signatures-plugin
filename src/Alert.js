const AlertTypes = {
  SUCCESS: 'success',
  ERROR: 'error',
}

class Alert {
  constructor(alertType, width) {
    this.height = height;
    this.width = width;
  }
}

export default Alert;
export {AlertTypes};

